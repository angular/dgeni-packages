/**
 * @dgService templatePatterns
 * @kind object
 * @type {String[]}
 * @description
 * A collection of lodash patterns that are expanded using the currnet document
 * as `doc` to match against the available templates.
 */
module.exports = function templatePatterns() {
  return [];
};