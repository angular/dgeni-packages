import { ExportDoc } from './ExportDoc';

/**
 * This document represents an exported symbol that can have multiple overload declarations
 * e.g. an exported function.
 */
export abstract class OverloadableExportDoc extends ExportDoc {
}
